PIC(PowerIterationClustering)幂迭代聚类
（1）输入:一个按行归一化的关联矩阵W=D^{-1}A ,和期望聚类数k(A为关联矩阵，D为度矩阵)
（2）随机选取一个非零初始向量v(0)
（3）计算新的v(t+1)=W*v(t)/||W*v(t)||，设定阈值frcnt(t+1) = |v(t+1) - v(t)|
    (这一步分子其实将W*v(t)得到的新的特征向量归一化了)
（4 重复迭代（3）式，直到|frcnt(t+1)-frcnt(t)|-> 0 停止
（5）使用k-means对经特征向量变换的点进行聚类
（6）输出：类id和对应的点id

